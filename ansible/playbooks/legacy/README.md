Legacy migration and cutover playbooks live here.

They are not part of the current provisioning, bootstrap, or verification
surface. Use them only when you intentionally need historical Docker-to-
`Platform-Core` migration artifacts or evidence.
