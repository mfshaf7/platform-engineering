# Component Access

## Operator Path

- supported Windows/operator URL or state that no UI exists

## WSL Fallback

- `kubectl port-forward` or inspection commands

## Credentials

- where credentials come from
- which runbook owns reissue or recovery
