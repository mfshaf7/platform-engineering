# Version Attestation

Each deployed environment should be able to prove:

- which image digest is running
- which source revisions fed the artifact
- which platform revision approved the deployment
- whether runtime matches the approved manifest
