apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: platform-postgresql-credentials
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: SecretStore
    name: platform-vault
  target:
    name: platform-postgresql-credentials
    creationPolicy: Owner
  data:
    - secretKey: postgres-password
      remoteRef:
        key: platform/postgresql/prod/service
        property: postgres_password
    - secretKey: password
      remoteRef:
        key: platform/postgresql/prod/openproject
        property: password
