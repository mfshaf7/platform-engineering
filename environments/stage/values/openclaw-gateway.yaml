image:
  repository: ghcr.io/mfshaf7/openclaw-gateway
  tag: "replace-me-stage"

env:
  OPENCLAW_ENV: stage
  OPENCLAW_TELEGRAM_SHA: replace-me
  OPENCLAW_HOST_BRIDGE_SHA: replace-me
  OPENCLAW_PLATFORM_SHA: replace-me

resources:
  requests:
    cpu: 250m
    memory: 512Mi
  limits:
    cpu: "1"
    memory: 1Gi
