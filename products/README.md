# Products

This directory holds product-specific integration contracts that sit on top of
the shared platform.

Use a separate `products/<product>/` directory when a product needs its own:

- runtime contract
- dependencies
- host integration notes
- secret and deployment expectations

Shared platform rules belong in [docs/standards](../docs/standards), not here.
