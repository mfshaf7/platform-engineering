# Image Attestation

Target controls:

- generate SBOMs during CI
- sign or attest published OCI artifacts
- record image digest in environment version files
- verify runtime-reported digest against the approved manifest
